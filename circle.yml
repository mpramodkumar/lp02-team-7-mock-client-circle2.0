machine:
  node:
    version: 8.7.0
test:
  pre:
    - npm run lint
    - npm run sass-lint
    - yarn install
  override:
    - yarn test:once
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/junit.xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
